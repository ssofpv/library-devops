# Проверка установки из чистой копии

Проверяющая: Макарычева С. А.

Проверяемый коммит:
44e9d03512df7aac41ad1fe5054f3f8ed98debe0

## Условия проверки

Проект клонирован в отдельную папку library-devops-clean-check.
Созданы новое виртуальное окружение, локальный .env
и отдельная база SQLite. Зависимости установлены по README.

## Подтверждённые результаты

| Проверка | Результат |
|---|---|
| Запуск приложения | Сервер запущен, HTTP-запросы обрабатываются |
| GET /health/ | Ответ {"status":"ok"} |
| GET /api/authors/ | HTTP 200 OK, пустой список |
| Ruff | All checks passed |
| Проверка форматирования | 41 файл соответствует форматированию |
| Django check | Ошибок не обнаружено |
| Проверка недостающих миграций | No changes detected |
| Автоматические тесты | 50 тестов, все завершились успешно |
| git status --short после установки | Вывод пустой |

## Вывод

Установка и запуск в отдельной копии выполнены успешно.
Обязательные проверки make verify пройдены.
Подтверждены доступность служебного адреса и получение списка
авторов из новой базы данных.

Эта проверка подтверждает установку и базовый запуск.
Полный ручной сценарий операций описан отдельно
в manual-check.md.